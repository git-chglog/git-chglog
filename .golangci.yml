# https://golangci-lint.run/usage/configuration/
run:
  timeout: 2m

output:
  format: tab

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

linters:
  enable:
    - dupl
    - depguard
    - gocritic
    - gocyclo
    - gofmt
    - golint
    - misspell
    - unconvert
